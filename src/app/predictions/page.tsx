"use client";

import React, { useState } from 'react';
import { Shell } from '@/components/layout/Shell';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { 
  TrendingUp, 
  TrendingDown, 
  Minus,
  BrainCircuit,
  Zap,
  RefreshCcw,
  Target,
  BarChart2
} from 'lucide-react';
import { MOCK_PREDICTIONS } from '@/app/lib/mock-data';
import { toast } from '@/hooks/use-toast';

export default function PredictionsPage() {
  const [loading, setLoading] = useState(false);

  const handleRefresh = () => {
    setLoading(true);
    setTimeout(() => {
      setLoading(false);
      toast({
        title: "Predictions Updated",
        description: "New market trajectory analysis generated by AI.",
      });
    }, 1500);
  };

  return (
    <Shell>
      <div className="space-y-8">
        <div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
          <div>
            <h1 className="text-3xl font-headline font-bold flex items-center gap-3">
              Market Predictions
              <BrainCircuit className="w-8 h-8 text-primary" />
            </h1>
            <p className="text-muted-foreground">AI-driven price trajectory analysis and target forecasting.</p>
          </div>
          <Button onClick={handleRefresh} disabled={loading} className="bg-primary hover:bg-primary/90 text-white gap-2 rounded-full">
            <RefreshCcw className={`w-4 h-4 ${loading ? 'animate-spin' : ''}`} />
            Re-calculate
          </Button>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {MOCK_PREDICTIONS.map((pred, i) => (
            <Card key={i} className="glass-card border-white/5 hover:border-primary/20 relative overflow-hidden group">
              <div className="absolute top-0 right-0 p-4">
                <div className={`p-2 rounded-lg ${pred.trend === 'Bullish' ? 'text-green-500 bg-green-500/10' : pred.trend === 'Bearish' ? 'text-destructive bg-destructive/10' : 'text-muted-foreground bg-white/5'}`}>
                  {pred.trend === 'Bullish' ? <TrendingUp className="w-5 h-5" /> : pred.trend === 'Bearish' ? <TrendingDown className="w-5 h-5" /> : <Minus className="w-5 h-5" />}
                </div>
              </div>
              <CardHeader>
                <div className="flex flex-col gap-1">
                  <Badge variant="outline" className="w-fit text-[10px] tracking-widest border-white/10 uppercase mb-2">
                    72H Forecast
                  </Badge>
                  <CardTitle className="text-3xl font-headline font-bold">{pred.asset}</CardTitle>
                </div>
              </CardHeader>
              <CardContent className="space-y-6">
                <div className="space-y-1">
                  <div className="flex justify-between items-end">
                    <p className="text-sm text-muted-foreground">Predicted Target</p>
                    <p className="text-2xl font-bold font-mono">${pred.targetPrice.toLocaleString()}</p>
                  </div>
                  <div className="h-1.5 w-full bg-white/5 rounded-full overflow-hidden">
                    <div 
                      className="h-full bg-primary animate-pulse" 
                      style={{ width: `${pred.confidence}%` }} 
                    />
                  </div>
                  <p className="text-[10px] text-right text-muted-foreground uppercase tracking-widest">{pred.confidence}% Confidence Level</p>
                </div>

                <div className="p-4 rounded-2xl bg-white/5 border border-white/5 flex items-center justify-between">
                  <div className="flex items-center gap-3">
                    <Target className="w-5 h-5 text-secondary" />
                    <div>
                      <p className="text-xs text-muted-foreground uppercase tracking-wider">Trend Strength</p>
                      <p className="text-sm font-bold">{pred.trend} Momentum</p>
                    </div>
                  </div>
                  <Zap className="w-5 h-5 text-secondary fill-current animate-pulse" />
                </div>

                <div className="pt-4 flex gap-3">
                  <Button variant="outline" className="flex-1 border-white/10 hover:bg-white/5 text-xs h-10 rounded-xl">
                    View Chart
                  </Button>
                  <Button className="flex-1 bg-primary text-white hover:bg-primary/90 text-xs h-10 rounded-xl">
                    Set Alert
                  </Button>
                </div>
              </CardContent>
            </Card>
          ))}
        </div>

        {/* Prediction Methodology */}
        <Card className="glass-card border-primary/10">
          <CardContent className="p-8">
            <div className="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
              <div>
                <h3 className="text-2xl font-headline font-bold mb-4">How it works</h3>
                <p className="text-muted-foreground leading-relaxed mb-6">
                  Our predictions are generated by combining three distinct AI models:
                  Sentiment Analysis (X & News), Technical Analysis Neural Networks, and On-chain Whale Movement detection.
                </p>
                <div className="space-y-4">
                  {[
                    'Dynamic market volume analysis',
                    'Global economic indicators integration',
                    '24/7 autonomous learning cycles',
                  ].map((item, i) => (
                    <div key={i} className="flex items-center gap-3">
                      <div className="w-5 h-5 rounded-full bg-primary/20 flex items-center justify-center">
                        <BarChart2 className="w-3 h-3 text-primary" />
                      </div>
                      <span className="text-sm font-medium">{item}</span>
                    </div>
                  ))}
                </div>
              </div>
              <div className="relative">
                <div className="absolute inset-0 bg-primary/20 blur-[80px] rounded-full" />
                <img 
                  src="https://picsum.photos/seed/ai/600/400" 
                  alt="AI Prediction Graph" 
                  className="rounded-3xl relative z-10 border border-white/10"
                />
              </div>
            </div>
          </CardContent>
        </Card>
      </div>
    </Shell>
  );
}